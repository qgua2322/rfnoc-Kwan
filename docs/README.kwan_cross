This is the kwan_cross-write-a-block package meant as a guide to building
out-of-tree packages. To use the kwan_cross blocks, the Python namespaces
is in 'kwan_cross', which is imported as:

    import kwan_cross

See the Doxygen documentation for details about the blocks available
in this package. A quick listing of the details can be found in Python
after importing by using:

    help(kwan_cross)
